 
# CMake uninstall script

# Delete installed files
foreach(file
        "@CMAKE_INSTALL_PREFIX@/lib/libzlog.a"
        "@CMAKE_INSTALL_PREFIX@/include/log.h"
        "@CMAKE_INSTALL_PREFIX@/include/logimpl.h"
    )
    file(REMOVE ${file})
endforeach()
